"""Running plot state for a single novel (scaffold only).

Purpose: track characters, events, and open setups (伏笔) across chapters so
later chapters stay consistent with earlier ones — the mechanism for 剧情连贯性.
"""
